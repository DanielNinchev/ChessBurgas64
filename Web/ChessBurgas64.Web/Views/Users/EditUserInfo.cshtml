@model ChessBurgas64.Web.ViewModels.Users.UserInputModel
@using ChessBurgas64.Common
@using ChessBurgas64.Data.Models.Enums
@{
    ViewData["Title"] = GlobalConstants.EditUsersProfileData;
}

<h1 class="display-5 text-white text-center animated slideInDown">@this.ViewData["Title"]</h1>
<hr class="border border-5 border-white text-white" />

<form method="post" enctype="multipart/form-data" class="mt-5 text-center">
    <div asp-validation-summary="All" class="text-danger"></div>
    <div class="row text-center">
        <div class="form-group mb-5">
            <label class="text-white" asp-for="ClubStatus">@GlobalConstants.ClubStatus: </label>
            <select asp-for="ClubStatus" asp-items="Html.GetEnumSelectList<ClubStatus>()">
                <option selected="selected" value="">@GlobalConstants.ClubStatusChoice</option>
            </select>
            <span asp-validation-for="ClubStatus" class="text-danger"></span>
        </div>
        <div class="form-group mb-5">
            <label class="text-white" asp-for="FideTitle">@GlobalConstants.FideTitle: </label>
            <select asp-for="FideTitle" asp-items="Html.GetEnumSelectList<FideTitle>()">
                <option selected="selected" value="">@GlobalConstants.FideTitleChoice</option>
            </select>
            <span asp-validation-for="FideTitle" class="text-danger"></span>
        </div>
        <div class="form-group mb-5 col-2 offset-5">
            <label class="text-white" asp-for="FideRating">@GlobalConstants.FideRating</label>
            <input asp-for="FideRating" class="form-control" />
            <span asp-validation-for="FideRating" class="text-danger"></span>
        </div>
    </div>
    <button type="submit" class="btn-primary">@GlobalConstants.Refresh</button>
</form>
